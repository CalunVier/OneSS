/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['mystatic.dzaaaaaa.com', 'image.huarzone.com', 'random.huarzone.com'],
    },
    reactStrictMode: true,
}

module.exports = nextConfig
